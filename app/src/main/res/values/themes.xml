<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OriginalColor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/my_light_primary</item>
        <!--Main Background-->
        <item name="android:colorBackground">@color/white</item>
        <!--Top App Bar Background-->
        <item name="colorSurface">@color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="chipStyle">@style/Widget.Material3.Chip.Filter</item>
    </style>

    <style name="Theme.OriginalColor" parent="Base.Theme.OriginalColor" />
    <style name="BlurViewStyle">
        <item name="android:background">@drawable/bg_blur_overlay</item>
    </style>
    <style name="ColorNameText">
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="ColorInfoText">
        <item name="drawableEndCompat">@drawable/ic_copy</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:clickable">true</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:padding">8dp</item>
    </style>

    <style name="Widget.App.Switch" parent="Widget.Material3.CompoundButton.MaterialSwitch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <style name="ThemeOverlay.App.Switch" parent="">
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorOutline">#80EE6C4D</item>
        <item name="trackDecorationTint">@color/my_light_primary</item>
    </style>
</resources>